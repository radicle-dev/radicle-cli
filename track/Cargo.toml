[package]
name = "rad-track"
version = "0.1.0"
authors = ["The Radicle Team <dev@radicle.xyz>"]
edition = "2018"
license = "MIT OR Apache-2.0"
description = "Track radicle project peers"

[dependencies]
lexopt = { version = "0.2" }
log = { version = "0.4" }
radicle-tools = { path = "../" }
anyhow = { version = "1.0" }
colored = { version = "1.9" }
librad = { version = "0" }
rad-clib = { version = "0" }
rad-terminal = { path = "../terminal" }
rad-common = { path = "../common" }
